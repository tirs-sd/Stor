# دليل تشغيل متجر تِرس من الصفر (إعداد + نشر)

اتبع الخطوات بالترتيب بالظبط. كله ضغط أزرار، مفيش أي كود تكتبه. خصص 20 دقيقة.

---

## الجزء الأول: قاعدة البيانات (مرة واحدة فقط)

### 1) إنشاء المشروع
1. افتح https://console.firebase.google.com وسجّل دخول بحساب جوجل (Gmail)
2. اضغط **Add project** (أو "إنشاء مشروع") → اكتب اسم زي `tars-store` → التالي → التالي → **Create project**
3. استنى شوية لحد ما يظهر "Your project is ready" → اضغط **Continue**

### 2) تفعيل قاعدة البيانات (Firestore)
1. من القائمة الجانبية اليسار: **Build** → **Firestore Database**
2. اضغط **Create database**
3. اختر **Start in production mode** → Next
4. اختر أقرب موقع سيرفر (لو ظاهر `eur3` أو `me-central1` اختاره) → **Enable**
5. بعد ما تفتح، روح لتبويب **Rules** فوق الصفحة، امسح كل النص الموجود، والصق هذا بالظبط:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isStaff() { return request.auth != null && exists(/databases/$(database)/documents/employees/$(request.auth.uid)); }

    match /products/{doc} {
      allow read: if true;
      allow create, delete: if isStaff();
      allow update: if isStaff() || (
        request.resource.data.diff(resource.data).affectedKeys().hasOnly(['qty','stock']) &&
        request.resource.data.qty <= resource.data.qty
      );
    }
    match /settings/{doc}      { allow read: if true; allow write: if isStaff(); }
    match /coupons/{doc}       { allow read: if true; allow write: if isStaff(); }
    match /orders/{doc}        { allow create: if true; allow read, update, delete: if isStaff() || (request.auth != null && resource.data.customerUid == request.auth.uid); }
    match /tickets/{doc}       { allow create: if true; allow read, update: if isStaff() || (request.auth != null && resource.data.customerUid == request.auth.uid); allow delete: if isStaff(); }
    match /invoices/{doc}      { allow read, write: if isStaff(); }
    match /events/{doc}        { allow create: if true; allow read: if isStaff(); }
    match /notifications/{doc} { allow read, write: if isStaff(); }
    match /employees/{uid} {
      allow read, update, delete: if isStaff();
      allow create: if isStaff() || (request.auth != null && request.auth.uid == uid && request.auth.token.email == 'admin@tirs.com');
    }
    match /customers/{uid}     { allow read, write: if request.auth != null && request.auth.uid == uid; allow read: if isStaff(); }
  }
}
```

> ⚠️ **تحديث أمني مهم:** لو كنت لصقت نسخة سابقة من هذه القواعد قبل كده، **لازم تروح تستبدلها بالنسخة الجديدة دي** وتضغط Publish تاني. النسخة القديمة كانت تسمح لأي حساب مسجّل دخول (حتى حساب عميل عادي) بقراءة/تعديل بيانات الموظفين. النسخة الجديدة تربط الصلاحيات بحساب الموظف الحقيقي فقط.
6. اضغط **Publish** فوق يمين

### 3) تفعيل تسجيل الدخول (Authentication)
1. من القائمة الجانبية: **Build** → **Authentication** → **Get started**
2. من تبويب **Sign-in method** اضغط **Email/Password** → فعّله (Enable) → **Save**

> ملاحظة: مفيش داعي تضيف مستخدم يدوي هنا — أول ما تفتح لوحة التحكم لأول مرة، هتظهر لك شاشة "الإعداد الأول" وتنشئ حساب المدير تلقائيًا بنفسك (البريد اللي طلبته: `admin@tirs.com` وكلمة السر `123456`، وتقدر تغيّرها بعدين).

### 4) تفعيل تخزين الصور (Storage)
1. من القائمة الجانبية: **Build** → **Storage** → **Get started** → اختر نفس موقع السيرفر → **Done**
2. روح لتبويب **Rules** والصق:
```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} { allow read: if true; allow write: if request.auth != null; }
  }
}
```
3. **Publish**

> لو الزر يطلب منك تفعيل خطة الدفع (Blaze)، ده بس لو المتجر كبر جدًا مستقبلًا. للاستخدام العادي الخطة المجانية (Spark) كفاية.

### 5) نسخ مفاتيح الربط
1. اضغط أيقونة الترس ⚙️ جنب "Project Overview" أعلى يسار → **Project settings**
2. انزل لقسم **Your apps** → اضغط أيقونة الويب `</>`
3. اكتب اسم زي `tars-web` → **Register app**
4. هيظهر كائن `firebaseConfig` — انسخه كامل، شكله كده:
```
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "tars-store.firebaseapp.com",
  projectId: "tars-store",
  storageBucket: "tars-store.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

### 6) لصق المفاتيح
1. افتح ملف `firebase-config.js` اللي بعته لك (بأي محرر نصوص، حتى المفكرة/Notepad تكفي)
2. امسح علامتي التنصيص الفاضية `""` قدام كل سطر، والصق قيمتك مكانها بالظبط (بين علامتي التنصيص)
3. احفظ الملف بنفس الاسم

**كيف تتأكد إنك ظبطت الملف صح؟** افتح لوحة-التحكم.html بعد كده — لو شفت شاشة تسجيل الدخول العادية (مش رسالة تحذير صفراء) يبقى الملف مظبوط صح.

---

## الجزء الثاني: النشر (تفعيل رابط حقيقي)

الملفات دي لازم تكون على "استضافة" (سيرفر متصل بالإنترنت) عشان تشتغل صح وتفضل شغالة بعد أي تحديث صفحة، وعشان أي عميل يقدر يفتحها من جواله. **فتح الملف بضغطتين من جهازك مش كافي.**

أسهل طريقة مجانية بدون أي كود (10 دقائق):

### النشر عبر Netlify (موصى بها)
1. رتّب الملفات دي بهذا الهيكل بالظبط داخل **مجلد واحد** (زي هيكل مشروعك بالظبط):
```
مجلد مشروعك/          ← افتح هذا المجلد في VS Code
├── index.html
├── لوحة-التحكم.html
├── firebase-config.js
├── دليل-الإعداد-والنشر.md
├── css/
│   ├── store.css
│   └── admin.css
└── js/
    ├── store.js
    └── admin.js
```
   يعني: `index.html`، `لوحة-التحكم.html`، و`firebase-config.js` في جذر المجلد مباشرة (مش جوّه أي مجلد فرعي زي `admin`). و`store.css`/`admin.css` جوّه مجلد `css`. و`store.js`/`admin.js` جوّه مجلد `js`.
2. افتح https://app.netlify.com/drop
3. سجّل حساب مجاني (بجوجل مثلاً) لو طلب منك
4. اسحب **مجلد المشروع بالكامل** (بمجلدي css وjs جواه) وأفلته بمنتصف الصفحة (Drag & Drop)
5. بعد ثواني هيديك رابط مباشر زي: `https://your-site-123.netlify.app`
6. **رابط المتجر (الصفحة الرئيسية مباشرة):** `https://your-site-123.netlify.app`
   **رابط لوحة التحكم:** `https://your-site-123.netlify.app/لوحة-التحكم.html`

هذا الرابط ثابت وحقيقي — ابعته لعملائك، واحتفظ برابط اللوحة لنفسك وموظفينك بس.

> **تحديث لاحق:** أي مرة تعدّل بالملفات، روح لنفس الموقع بـ Netlify → **Deploys** → اسحب المجلد المحدث تاني، بياخد ثواني.

### بديل: لو عندك استضافة (cPanel) جاهزة
ارفع نفس الهيكل (بمجلدي `css` و`js` بالظبط) عبر File Manager أو FTP لمجلد `public_html`، وهتشتغل بنفس رابط دومينك — بما إن الملف الرئيسي اسمه `index.html`، هيفتح تلقائيًا من غير ما تحتاج تكتب اسم الملف.

---

## تأكيد إن كل حاجة شغالة
1. افتح رابط لوحة التحكم → هتظهر شاشة "الإعداد الأول" (لو أول مرة) → اعمل الحساب → سجّل دخول
2. افتح رابط المتجر بتاب تاني → جرّب تسجّل حساب عميل جديد → أضف منتج للسلة → أكمل الطلب
3. ارجع للوحة التحكم → لازم تلاقي الطلب ظهر فورًا بدون ما تعمل أي حاجة يدوية

لو أي خطوة ما ظبطت، ابعتلي سكرين شوت وأنا أوجهك بالظبط لمكان المشكلة.
